name: MAGMA API Auto Restart

on:
  workflow_run:
    workflows:
      - "MAGMA Music API"
    types:
      - completed

permissions:
  actions: write
  contents: read

jobs:
  restart:
    # VERY IMPORTANT:
    # Only react to MAGMA Music API.
    # Never react to this Auto Restart workflow itself.
    if: >
      github.event.workflow_run.name == 'MAGMA Music API' &&
      github.event.workflow_run.conclusion == 'timed_out'

    runs-on: ubuntu-latest

    steps:

      - name: Show Previous Run
        run: |
          echo "======================================"
          echo "       MAGMA AUTO RESTART"
          echo "======================================"

          echo "Workflow:"
          echo "${{ github.event.workflow_run.name }}"

          echo "Conclusion:"
          echo "${{ github.event.workflow_run.conclusion }}"

          echo "Run ID:"
          echo "${{ github.event.workflow_run.id }}"


      - name: Wait Before Restart
        run: |
          echo "Waiting 20 seconds..."
          sleep 20


      - name: Start MAGMA Music API Again
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e

          echo "======================================"
          echo "     STARTING MAGMA MUSIC API"
          echo "======================================"

          gh workflow run main.yml \
            --repo "${{ github.repository }}" \
            --ref main

          echo ""
          echo "✅ New MAGMA Music API run started."